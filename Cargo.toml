[package]
name = "handterm"
version = "0.1.0"
edition = "2024"
build = "build.rs"
description = "Wayland-native terminal focused on low latency"
license = "MIT"
repository = "https://github.com/1jehuang/handterm"

[workspace]
members = [
    ".",
    "handterm-common",
    "handterm-server",
    "handterm-client",
    "handterm-client-cpu",
    "handterm-client-gpu",
]
resolver = "2"

[dependencies]
anyhow = "1.0.95"
clap = { version = "4.5.28", features = ["derive"] }
dirs = "6.0.0"
freetype-rs = { version = "0.38", optional = true }
fontconfig = { version = "0.7", optional = true }
handterm-common = { path = "handterm-common" }
nix = { version = "0.30.1", features = ["term", "process", "signal", "fs", "poll"] }
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0"
softbuffer = { version = "0.4.6", optional = true }
toml = "0.8.20"
unicode-width = "0.2"
unicode-segmentation = "1.12.0"
rustybuzz = { version = "0.20", optional = true }
wgpu = { version = "28.0.0", optional = true }
bytemuck = { version = "1.21", features = ["derive"], optional = true }
pollster = { version = "0.4", optional = true }
bincode = "1.3.3"

[target.'cfg(target_os = "linux")'.dependencies]
winit = { version = "0.30.9", default-features = false, features = ["wayland", "rwh_06", "wayland-dlopen"] }

[target.'cfg(not(target_os = "linux"))'.dependencies]
winit = { version = "0.30.9", default-features = false, features = ["rwh_06"] }

[features]
default = ["cpu", "gpu", "local-fonts", "ligatures", "standalone", "daemon-client", "daemon-server", "cli"]
cpu = ["dep:softbuffer"]
gpu = ["dep:wgpu", "dep:bytemuck", "dep:pollster"]
local-fonts = ["dep:freetype-rs", "dep:fontconfig"]
ligatures = ["dep:rustybuzz", "local-fonts"]
standalone = []
daemon-client = []
daemon-server = []
cli = []

[dev-dependencies]
assert_cmd = "2.0.16"
predicates = "3.1.3"
tempfile = "3.15.0"

[profile.release]
lto = true
strip = true
codegen-units = 1
