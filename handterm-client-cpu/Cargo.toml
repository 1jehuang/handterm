[package]
name = "handterm-client-cpu"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
anyhow = "1.0.95"
clap = { version = "4.5.28", features = ["derive"] }
handterm = { path = "..", default-features = false, features = ["daemon-client", "cpu"] }

[[bin]]
name = "handterm-client-cpu"
path = "src/main.rs"
